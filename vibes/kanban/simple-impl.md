# 最小限の実装 フェーズ

## 0. 条件
- `[SIMPLE IMPL]` モードで動いていることを明示する。
- `vibes/tmp/{ブランチ名}/design.md` を読み、今回のターンで扱う変更箇所 / テスト / 副作用の 3 点セットを把握する。

## 1. 前提
- use context7

## 2. 実装（TDD）
1. 新しいテストまたは既存テストの強化を記述し、失敗させる。
2. テストが通る最小限の実装を追加し、再度テストを実行する。
3. テストが通れば副作用チェック（ログ、画面、API など）を行う。

## 3. レポート
以下の小見出しを必ず含める。
- 実施内容: 実装・調整したポイントを箇条書き。
- テスト結果: 実行したコマンドと結果、追加検証があれば詳細。
- 残課題: 未対応項目、リスク、`OTHER TASK` へ送るべき内容。

