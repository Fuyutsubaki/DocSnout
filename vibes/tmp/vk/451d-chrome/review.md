# Review Checklist

この変更は「最小のChrome拡張土台」のみを対象とする。

## MUST

- `manifest.json` が Manifest V3 である
- ツールバーに拡張アイコンが表示される（`action` + `icons` が設定されている）
- ポップアップで固定文言「拡張が有効です」が表示される
- ページ解析・保存機能を追加していない（権限や content script 等が増えていない）

